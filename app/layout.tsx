import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Heed & Brim Care Services | NDIS Provider Victoria',
  description: 'Person-centered NDIS care services in Victoria, Australia. Empowering individuals with disabilities to live meaningful, independent, and fulfilling lives.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
